<%= form_for(@mer20) do |f| %>
  <% if @mer20.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mer20.errors.count, "error") %> prohibited this mer20 from being saved:</h2>

      <ul>
      <% @mer20.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sequence %><br>
    <%= f.text_field :sequence %>
  </div>
  <div class="field">
    <%= f.label :strand %><br>
    <%= f.text_field :strand %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
